const Products = require('./products.models'); //? Import the Products model

// Establish your models relations inside this function
const initModels = () => {
    //? Define your relations here
    Products
};

module.exports = initModels;